[project]
name = "my-tools-client"
version = "0.1.0"
description = "Prometheus+Grafana dashboard for my_tools"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.138.1",
    "jinja2<3.1",
    "prometheus-client>=0.25.0",
    "sse-starlette>=3.4.5",
    "uvicorn>=0.49.0",
]
